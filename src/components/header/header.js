import React from 'react';

//header é um component
//cria  uma pasta somente para ele porque vai ficar todos os arquivos dele em um só lugar


//para importar o css, venho direto aqui tambem

import './header.css';

//stateless components  - criamos componentes por meio  de variaveis
//pode tirar os parenteses se desejar

const   Header = () => (
    <header id="main-header"> Sistema </header>
    
);

export default Header;